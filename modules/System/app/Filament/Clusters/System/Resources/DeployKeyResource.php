<?php

namespace Modules\System\Filament\Clusters\System\Resources;

use Filament\Forms;
use Filament\Forms\Form;
use Filament\Resources\Resource;
use Filament\Tables;
use Filament\Tables\Table;
use Modules\System\Filament\Clusters\System\Resources\DeployKeyResource\Pages;
use Modules\System\Filament\Clusters\System\Resources\DeployKeyResource\RelationManagers;
use Modules\System\Models\DeployKey;


class DeployKeyResource extends Resource
{
    protected static ?string $model = DeployKey::class;
    protected static ?string $navigationGroup = 'Galaxy-Network | System';
    protected static ?string $navigationIcon = 'heroicon-o-rectangle-stack';

    public static function form(Form $form): Form
    {
        return $form
            ->schema([
                Forms\Components\TextInput::make('name')
                    ->required()
                    ->maxLength(255),
                Forms\Components\TextInput::make('public_key')
                    ->required()
                    ->maxLength(255),
                Forms\Components\Textarea::make('private_key')
                    ->required()
                    ->columnSpanFull(),
            ]);
    }

    public static function table(Table $table): Table
    {
        return $table
            ->columns([
                Tables\Columns\TextColumn::make('id')
                    ->label('ID')
                    ->searchable(),
                Tables\Columns\TextColumn::make('name')
                    ->searchable(),
                Tables\Columns\TextColumn::make('public_key')
                    ->searchable(),
                Tables\Columns\TextColumn::make('created_at')
                    ->dateTime()
                    ->sortable()
                    ->toggleable(isToggledHiddenByDefault: true),
                Tables\Columns\TextColumn::make('updated_at')
                    ->dateTime()
                    ->sortable()
                    ->toggleable(isToggledHiddenByDefault: true),
            ])
            ->filters([
                //
            ])
            ->actions([
                Tables\Actions\EditAction::make(),
            ])
            ->bulkActions([
                Tables\Actions\BulkActionGroup::make([
                    Tables\Actions\DeleteBulkAction::make(),
                ]),
            ]);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => Pages\ListDeployKeys::route('/'),
            'create' => Pages\CreateDeployKey::route('/create'),
            'edit' => Pages\EditDeployKey::route('/{record}/edit'),
        ];
    }
}

